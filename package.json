{
  "name": "epson-tm-t88v-emulator",
  "version": "0.0.1",
  "description": "Emulator of the Epson TM-T88V thermal receipt printer",
  "license": "BSD-3-Clause",
  "author": "",
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --loader:.png=dataurl --format=esm --outdir=dist && tsc"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "escpos-ts": "file:../escpos-ts"
  }
}
